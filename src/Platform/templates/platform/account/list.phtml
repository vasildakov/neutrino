<div class="container-fluid">

    <div class="row p-4 bg-white">
        <div class="col-12">
            <h1 class="h4">Accounts</h1>
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Roles</th>
                    <th>Created At</th>
                    <th>Action</th>
                </tr>
                </thead>
                <tbody>

                <?php foreach ($this->users as $account): ?>
                    <tr>
                        <td><?= $account->getShortId() ?></td>
                        <td><?= $account->getFullName() ?></td>
                        <td><?= $account->getEmail() ?></td>
                        <td><?= implode(', ', $account->getRoles()->toArray()) ?></td>
                        <td><?= $account->getCreatedAt()->format('Y-m-d h:i:s'); ?></td>
                        <td>Action</td>
                    </tr>
                <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
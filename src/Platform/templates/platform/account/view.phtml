<?php
/**
 * @var \Neutrino\Domain\User\User $user
 * @psalm-var \Neutrino\Domain\User\User $user
 * @var array<\Neutrino\Domain\User\UserActivity> $activities
 */

// Helper function for escaping HTML
$esc = fn(string $v): string => htmlspecialchars($v, ENT_QUOTES, 'UTF-8');
?>
<div class="row mb-3">
    <div class="col-md-6 d-flex">
        <div class="tile tile-body w-100">
            <h5 class="fw-bold">User Details</h5>
            <?php if ($user->getAvatar()): ?>
            <div class="text-center mb-3">
                <img src="<?=$user->getAvatar()?>" alt="<?=$user->getFullName()?>" class="rounded-circle" style="width: 100px; height: 100px; object-fit: cover;">
            </div>
            <?php endif; ?>
            <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>User ID:</strong>
                    <code><?=$user->getShortId()?></code>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>Email:</strong>
                    <span><?=$user->getEmail()?></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>Name:</strong>
                    <span><?=$user->getName()?></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>Surname:</strong>
                    <span><?=$user->getSurname()?></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>Full Name:</strong>
                    <span><?=$user->getFullName()?></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>Created:</strong>
                    <span><?=$user->getCreatedAt()->format('Y-m-d H:i:s')?></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <strong>Platform Access:</strong>
                    <span>
                        <?php if ($user->hasPlatformRole()): ?>
                            <span class="badge bg-success">Yes</span>
                        <?php else: ?>
                            <span class="badge bg-secondary">No</span>
                        <?php endif; ?>
                    </span>
                </li>
            </ul>
        </div>
    </div>
    <div class="col-md-6 d-flex">
        <div class="tile tile-body w-100">
            <h5 class="fw-bold">User Roles</h5>
            <div class="table-responsive">
                <table class="table table-hover table-striped">
                    <thead>
                        <tr>
                            <th>Role</th>
                            <th>Scope</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php
                        /** @var \Neutrino\Domain\User\Role $role */
                        foreach($user->getRoles() as $role): ?>
                            <tr>
                                <td><?=$role->name()?></td>
                                <td><?=$role->getScope()?></td>
                            </tr>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<?php $membership = $user->getAccountMemberships()->first(); ?>

<div class="row mb-3">
    <div class="col-md-12">
        <div class="tile tile-body">
            <h5 class="fw-bold">Account Membership</h5>
            <?php if (!$membership): ?>
                <p class="text-muted">No memberships are associated with this account.</p>
            <?php else: ?>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Account ID</th>
                            <th>Account Name</th>
                            <th>Role</th>
                            <th>Member Since</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td><code><?=substr((string) $membership->account()->id(), 0, 8)?></code></td>
                            <td><?=$membership->account()->name()?></td>
                            <td><span class="badge bg-primary"><?=$membership->role()->value?></span></td>
                            <td><?=$membership->createdAt()->format('Y-m-d H:i:s')?></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<div class="row mb-3">
    <div class="col-md-12">
        <div class="tile tile-body">
            <h5 class="fw-bold">Account Subscription</h5>
            <?php $subscription = ($membership) ? $membership->account()->subscription() : null; ?>
            <?php if (!$subscription): ?>
                <p class="text-muted">No active subscription for this account.</p>
            <?php else: ?>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Plan</th>
                            <th>Status</th>
                            <th>Amount</th>
                            <th>Provider</th>
                            <th>Current Period</th>
                            <th>Created</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td><strong><?=$subscription->plan() ?? 'N/A'?></strong></td>
                            <td>
                                <?php if ($subscription->isActive()): ?>
                                    <span class="badge bg-success"><?=ucfirst($subscription->status())?></span>
                                <?php else: ?>
                                    <span class="badge bg-secondary"><?=ucfirst($subscription->status())?></span>
                                <?php endif; ?>
                            </td>
                            <td>
                                <?php if ($subscription->amount() && $subscription->currency()): ?>
                                    <?=number_format((float)$subscription->amount(), 2)?> <?=strtoupper($subscription->currency())?>
                                <?php else: ?>
                                    N/A
                                <?php endif; ?>
                            </td>
                            <td><span class="badge bg-info"><?=ucfirst($subscription->provider())?></span></td>
                            <td>
                                <?php if ($subscription->currentPeriodStart() && $subscription->currentPeriodEnd()): ?>
                                    <?=$subscription->currentPeriodStart()->format('Y-m-d')?> - <?=$subscription->currentPeriodEnd()->format('Y-m-d')?>
                                <?php else: ?>
                                    N/A
                                <?php endif; ?>
                            </td>
                            <td><?=$subscription->createdAt()->format('Y-m-d H:i:s')?></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <?php endif; ?>
        </div>
    </div>
</div>


<?php $stores = ($membership) ? $membership->account()->getStores() : null; ?>

<div class="row mb-3">
    <div class="col-md-12">
        <div class="tile tile-body">
            <h5 class="fw-bold">Stores <?php if ($stores): ?>(<?=$stores->count()?>)<?php endif; ?></h5>
            <?php if (!$stores || $stores->isEmpty()): ?>
                <p class="text-muted">No stores are associated with this account.</p>
            <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Slug</th>
                                <th>Domain</th>
                                <th>Database</th>
                                <th>Created</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach($stores as $store): ?>
                                <tr>
                                    <td><?=$store->name()?></td>
                                    <td><code><?=$store->slug()->getValue()?></code></td>
                                    <td><a href="https://<?=$store->domain()?>" target="_blank"><?=$store->domain()?></a></td>
                                    <td><?=$store->database() ? '✓' : '✗'?></td>
                                    <td><?=$store->getCreatedAt()->format('Y-m-d H:i')?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<div class="row mb-3">
    <div class="col-md-12">
        <div class="tile tile-body">
            <h5 class="fw-bold">Payment History</h5>
            <?php $payments = ($membership) ? $membership->account()->payments() : null; ?>
            <?php if (!$payments || $payments->isEmpty()): ?>
                <p class="text-muted">No payment history available.</p>
            <?php else: ?>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Description</th>
                                <th>Amount</th>
                                <th>Status</th>
                                <th>Provider</th>
                                <th>Provider Ref</th>
                                <th>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach($payments as $payment): ?>
                                <tr>
                                    <td><code><?=substr((string) $payment->id(), 0, 8)?></code></td>
                                    <td><?=$payment->description() ?? 'N/A'?></td>
                                    <td><strong><?=number_format((float)$payment->amount(), 2)?> <?=strtoupper($payment->currency())?></strong></td>
                                    <td>
                                        <?php
                                        $statusClass = match($payment->status()) {
                                            'succeeded', 'completed' => 'bg-success',
                                            'pending' => 'bg-warning',
                                            'failed', 'canceled' => 'bg-danger',
                                            default => 'bg-secondary'
                                        };
                                        ?>
                                        <span class="badge <?=$statusClass?>"><?=ucfirst($payment->status())?></span>
                                    </td>
                                    <td><span class="badge bg-info"><?=ucfirst($payment->provider())?></span></td>
                                    <td><?=$payment->providerRef() ? '<code>' . $payment->providerRef() . '</code>' : 'N/A'?></td>
                                    <td><?=$payment->createdAt()->format('Y-m-d H:i:s')?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php endif; ?>
        </div>
    </div>
</div>

<div class="row mb-3">
    <div class="col-md-12">
        <div class="tile tile-body">
            <h5 class="fw-bold">User Activity</h5>
            <?php if (!empty($activities)): ?>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Activity</th>
                                <th>Description</th>
                                <th>City</th>
                                <th>IP Address</th>
                                <th>Browser</th>
                                <th>Date & Time</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($activities as $activity): ?>
                                <tr>
                                    <td>
                                        <span class="badge <?= $activity->getBadgeClass() ?>">
                                            <?= $activity->getActivityLabel() ?>
                                        </span>
                                    </td>
                                    <td><?= $esc($activity->getDescription()) ?></td>
                                    <td><?= $esc($activity->getCity() ?? '-') ?></td>
                                    <td><?= $esc($activity->getIpAddress() ?? '-') ?></td>
                                    <td><?= $esc($activity->getBrowser()) ?></td>
                                    <td><?= $activity->getCreatedAt()->format('Y-m-d H:i:s') ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                </div>
            <?php else: ?>
                <p class="text-muted">No user activities recorded yet.</p>
            <?php endif; ?>
        </div>
    </div>
</div>